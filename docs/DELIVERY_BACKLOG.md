# NOVA INTERP 交付 Backlog

## 已完成

- Qt/Web 桌面控制台主界面
- 三通道基础模型：A / B / C
- 自定义暗色 UI、顶部工具栏、设置抽屉
- 双栏实时字幕区
- 设备枚举、场景模板、语言切换
- 延迟测量面板
- 声音复刻 V3 状态与试听链路
- Rust 偏置入口和高级调优入口
- Git 仓库初始化与 GitHub 远端同步

## 当前高优先级

1. Rust 原生音频核心真正接管关键输入输出链路
2. AGC / VAD / 降噪 / 噪声门稳定落地
3. 虚拟声卡链路下的回声、截断、短响问题继续收敛
4. 识别偏置模板扩展成可编辑词表
5. 更新清单与自动更新下载源打通
6. 安装包、签名和首次启动引导

## 中优先级

1. OBS / 浏览器字幕输出
2. SRT / VTT / JSON 多格式导出
3. 更多场景模板和语种模板
4. 更完整的运行诊断与日志打包
5. 音色模板标签和试听库管理

## 发布阻塞项

- 长时间运行稳定性验证
- 设备热插拔后的恢复能力
- 代理、DNS、自定义网络策略的兼容性
- 游戏语音和系统音效混路时的误触发与丢词
- 打包后资源路径、配置路径、更新路径一致性

## 验收标准

- `launch.ps1` 可以在干净环境拉起桌面应用
- `scripts/build_windows.ps1` 可以产出 zip 包
- GitHub Actions 可以在 Windows runner 上自动构建
- `config.local.json` 不会被误提交
- 主界面、设置抽屉、延迟抽屉能正常打开

## 建议交付顺序

1. 先稳住桌面打包和发布链路
2. 再做 Rust 音频核心与 DSP
3. 最后补安装器、签名和更新器闭环
