[package]
name = "nova-audio-core"
version = "0.1.0"
edition = "2021"

[dependencies]
cpal = "0.16"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
